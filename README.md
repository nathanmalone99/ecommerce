# Ecommerce

Full-stack e-commerce learning project.

## Structure

- `shopdb-api/` — NestJS middleware (auth, validation, JWT)
- `database/` — SQL Server schema and seed scripts
- (later) `api-spring/` — Spring Boot backend
- (later) `frontend-angular/` — Angular UI